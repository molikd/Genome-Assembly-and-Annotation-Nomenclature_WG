import re
import json
from jsonschema import validate

schema = {
}

class gaan:

    def __init__():

    def validate(self):
        gaan_instance = json.dumps(self.__dict__)
        validate(instance=gaan_instance, schema=schema)
